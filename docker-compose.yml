version: "3.9"

services:
  ydb:
    image: ydbplatform/local-ydb:25.2
    container_name: ydb-local
    hostname: localhost
    ports:
      - "2136:2136"
      - "8765:8765"
    environment:
      GRPC_PORT: 2136
      MON_PORT: 8765
    volumes:
      - ./ydb_data:/ydb_data